/**
 * Setup global para testes E2E.
 * Executado uma vez antes de todos os arquivos de teste via setupFiles.
 * Não sobe containers aqui — cada arquivo gerencia seu próprio ciclo via helpers.
 */

// Garante que reflect-metadata é carregado antes de qualquer decorador
import 'reflect-metadata';
