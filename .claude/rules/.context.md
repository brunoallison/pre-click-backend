<context path="rules" updated="2026-04-19">

Regras de constraint do projeto. Uma regra por arquivo, com `## Statement` (RFC-style: MUST/MUST NOT/SHOULD) e `## Rationale`.

## Summary

- git.md — conventional commits, branch naming, base em `main`, PRs sempre para `main`
- agents.md — saída de todos os agentes de revisão de CI em português brasileiro

## Constraints

- MUST NOT adicionar regras condicionais ou de escopo limitado aqui sem justificativa — regras aqui são universais
- MUST incluir `## Statement` e `## Rationale` em todo arquivo de regra

</context>
